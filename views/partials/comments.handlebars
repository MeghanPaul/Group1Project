<div class="comments">
  {{#each this}}
    <section class="comment">
      <div class="meta">
        {{created_at}}
      </div>
      <div class="text">
        {{text}}
      </div>
    </section>
  {{/each}}
</div>